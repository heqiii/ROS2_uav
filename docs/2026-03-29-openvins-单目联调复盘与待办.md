# 2026-03-29 OpenVINS 单目联调复盘与待办

## 今晚完成的工作

1. ROS2 侧脚本化与可运行性收敛
- 完成 `source_ros_env.sh` 的增强与重构，支持统一环境加载、错误提示、包可见性检查。
- 增加一键控制函数：`start_mono_vins`、`stop_mono_vins`、`status_mono_vins`。
- 增加基础运行函数：`run_imu_bridge`、`run_ov_mono`、`run_vio_to_px4`。

2. 权限与运行环境问题修复
- 处理了历史 `root` 运行导致的权限污染（`build/install/log/src` 等目录），恢复为 `he` 用户可持续运行。
- 修复 ROS2 Jazzy `setup.bash` 在严格 `set -u` 场景下的兼容问题（source 前后临时 `set +u/set -u`）。

3. 链路连通性问题定位与修复
- 修复 `imu_bridge` 与 PX4 DDS 的 QoS 不兼容问题，确保 `/fmu/out/sensor_combined -> /imu0` 可持续出数。
- 确认并修复 OpenVINS 与 `vio_to_px4` 的话题错配：
  - OpenVINS 发布 `/odomimu`
  - `vio_to_px4` 订阅从 `/ov_msckf/odomimu` 调整为 `/odomimu`
- 补齐并验证相机桥接后，`/cam0/image_raw`、`/cam0/camera_info` 均可见且有发布者。

4. 端到端实测结果沉淀
- 进程层：`imu_bridge`、`ov_msckf`、`vio_to_px4` 可稳定拉起。
- 话题层：
  - `/imu0` 频率约 95~103 Hz（稳定）
  - `/cam0/image_raw` 频率约 24~30 Hz（稳定）
  - `/odomimu` 拓扑已连通（发布/订阅关系正确）
  - `/fmu/in/vehicle_visual_odometry` 已有发布端与 PX4 订阅端

## 当前结论（截至今晚）

1. 已打通的部分
- 进程与通信链路基本打通：PX4 DDS、IMU、相机桥、VIO 回传节点均可运行。

2. 仍待解决的部分
- OpenVINS 尚未稳定产出可观测的 `Odometry` 消息（`/odomimu` 仍未确认持续输出）。
- 现象上表现为：节点在跑、订阅关系正确，但 `hz/echo` 未持续看到有效里程计样本。

3. 关键风险点
- 时间基一致性风险仍高：日志存在超大 `ms behind`，提示仿真时钟/消息时戳仍可能未完全对齐。
- 初始化阶段可观测性不足：若仿真运动激励不足（平移+转动不够），单目初始化可能长期不收敛。

## 详细待办（按优先级）

### P0（必须先完成）

1. 统一时钟基准并验证 `/clock`
- 在仿真侧确认 `gz -> ROS2` 时钟桥存在并稳定。
- ROS2 侧关键节点统一确认 `use_sim_time` 策略（OpenVINS、桥接节点、调试工具）。
- 验收标准：
  - `ros2 topic list` 可见 `/clock`
  - 各节点时间戳无数量级偏差（不再出现异常 `ms behind`）

2. 确认相机桥接方向与命名唯一
- 固定使用一套话题名（推荐保留 `/cam0/image_raw` + `/cam0/camera_info`）。
- 清理重复桥接命令，避免双桥导致时序/回环干扰。
- 验收标准：
  - `/cam0/image_raw` 仅有预期发布者
  - 频率稳定在约 20~30 Hz

3. OpenVINS 初始化可观测性验证
- 在仿真中执行可观测运动：小幅平移 + 偏航/俯仰变化，避免纯静止。
- 持续 30~60 秒观察 `/odomimu`。
- 验收标准：
  - `/odomimu` 持续发布
  - `ros2 topic hz /odomimu` 有稳定频率

### P1（链路确认）

4. 回传到 PX4 的一致性验证
- 在 `/odomimu` 稳定后，检查 `/fmu/in/vehicle_visual_odometry` 连续输出。
- PX4 侧执行：
  - `listener vehicle_visual_odometry 1`
  - `listener estimator_aid_src_ev_pos 1`
  - `listener estimator_aid_src_ev_yaw 1`
- 验收标准：
  - PX4 可持续收到 `vehicle_visual_odometry`
  - EV 位姿/航向融合状态正常

5. EKF2 参数复核（与当前 airframe 对齐）
- `EKF2_EV_CTRL=11`
- `EKF2_EV_DELAY=0`（必要时根据日志微调）
- `EKF2_HGT_REF=0`
- `EKF2_EV_POS_X/Y/Z=0`（后续按真实外参替换）
- 验收标准：
  - 无持续创新发散
  - 无连续 estimator reset

### P2（收尾与固化）

6. 固化可复现实验脚本
- 将“PX4 启动 / DDS Agent / 相机桥接 / ROS2 单目链路”整理为一键化步骤。
- 在文档中记录每一步“预期输出”和“失败排查命令”。

7. 输出最终联调报告
- 记录成功参数、话题频率、关键日志片段与剩余问题。
- 给出“可迁移到实机前”的最小准入清单。

## 建议的下一次执行顺序（明日直接照此跑）

1. PX4 SITL + DDS Agent + 相机桥接先起。
2. ROS2 执行 `start_mono_vins`。
3. 先看 `/cam0/image_raw`、`/imu0` 频率。
4. 做 30~60 秒可观测运动，观察 `/odomimu`。
5. 若 `/odomimu` 出数，再看 `/fmu/in/vehicle_visual_odometry` 与 PX4 listener。
